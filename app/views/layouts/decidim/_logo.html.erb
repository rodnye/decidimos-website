<% if organization %>
  <%= link_to decidim.root_url(host: organization.host) do %>
    <% if organization.logo.attached? %>
      <div class='w-64 h-64 sr-only'></div>
      <%= image_tag organization.attached_uploader(:logo).variant_url(:medium), alt: "#{current_organization_name} (#{t("decidim.errors.not_found.back_home")})", class: "w-64 h-64" %>
    <% else %>
      <span><%= current_organization_name %></span>
    <% end %>
  <% end %>
<% else %>
  <%= Decidim.application_name %>
<% end %>
